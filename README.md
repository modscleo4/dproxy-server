# dproxy-server
A Python implementation of a DProxy compatible server.

## Installation
```shell
uv sync --frozen
```

## Run
```shell
python main.py --address 0.0.0.0 --http-port 8080 --tcp-port 8081
```
